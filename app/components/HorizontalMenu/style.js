import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    main: {
        backgroundColor: '#FFF',
        // position: 'absolute',
        top: 0,
        zIndex: 1
    },
});

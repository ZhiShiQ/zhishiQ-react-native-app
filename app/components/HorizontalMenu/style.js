import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    main: {
        backgroundColor: '#F7F7F7',
        position: 'absolute',
        top: 0,
        zIndex: 1
    },
});

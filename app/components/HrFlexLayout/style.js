import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    main: {
        // flex: 1,
        justifyContent: 'center',
        flexDirection: 'row',
        flexWrap: 'wrap',
        // alignItems: 'center',
        // backgroundColor: '#F7F7F7'
    }
});
